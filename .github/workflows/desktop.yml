name: Desktop installers
on:
  workflow_dispatch:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
permissions:
  contents: read
concurrency:
  group: desktop-${{ github.ref }}
  cancel-in-progress: false
jobs:
  build:
    name: Build (${{ matrix.platform }})
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            platform: windows
            bundles: nsis
          - os: macos-latest
            platform: macos
            bundles: app,dmg
          - os: ubuntu-22.04
            platform: linux
            bundles: deb,appimage
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: npm
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
      - name: Linux native dependencies
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf libfuse2
      - run: python -m pip install -r requirements-build.txt
      - run: npm ci
      - run: python scripts/check_version.py
      - run: python scripts/sync_ui_assets.py --check
      - run: python scripts/check_glib_backport.py
      - run: python -m unittest discover -s tests -v
      - run: python scripts/build_sidecar.py
      - run: python scripts/smoke_sidecar.py
      - run: python scripts/smoke_nearby.py
      - name: Verify optimized Linux glib security regression
        if: runner.os == 'Linux'
        run: cargo test --locked --release --manifest-path src-tauri/Cargo.toml --test glib_variant_iter
      - name: Build unsigned validation packages
        if: "!startsWith(github.ref, 'refs/tags/')"
        run: npm run desktop:build -- --ci --bundles ${{ matrix.bundles }} --config src-tauri/unsigned.conf.json
      - name: Build signed update packages
        if: startsWith(github.ref, 'refs/tags/')
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
        run: npm run desktop:build -- --ci --bundles ${{ matrix.bundles }}
      - name: Verify Windows updater window
        if: runner.os == 'Windows'
        run: python scripts/check_windows_updater.py
      - name: Verify Windows installer upgrade and saved state
        if: runner.os == 'Windows'
        run: python scripts/check_windows_upgrade.py
      - name: Build isolated Linux native test app
        if: runner.os == 'Linux'
        run: |
          sudo apt-get install -y xvfb
          npm run desktop:build -- --debug --bundles appimage --config '{"identifier":"ca.lgraham.skilldesk.localtest","productName":"Skill-Desk Test","bundle":{"createUpdaterArtifacts":false}}'
      - name: Verify Linux WebKit interactions
        if: runner.os == 'Linux'
        run: xvfb-run -a python tests/run_native_smoke.py "$RUNNER_TEMP/skilldesk-native-evidence"
      - name: Preserve Linux native evidence
        if: always() && runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: linux-native-evidence
          path: ${{ runner.temp }}/skilldesk-native-evidence/
          if-no-files-found: warn
      - name: Collect installers
        run: python scripts/collect_release.py
      - uses: actions/upload-artifact@v4
        with:
          name: installer-${{ matrix.platform }}
          if-no-files-found: error
          path: release-assets/*
          retention-days: 14
  publish:
    name: Publish release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
      - uses: actions/download-artifact@v4
        with:
          pattern: installer-*
          merge-multiple: true
          path: release-assets
      - name: Publish installers and checksums
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ github.ref_name }}
        run: python3 scripts/publish_release.py
